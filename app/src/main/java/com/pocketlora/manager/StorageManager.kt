package com.pocketlora.manager

class StorageManager {
    // TODO: Handle saving and loading adapters from /adapters/ folder
    fun saveAdapter(name: String, data: ByteArray) {
        // Write adapter to storage
    }

    fun listAdapters(): List<String> {
        // Return available adapters
        return emptyList()
    }
}
